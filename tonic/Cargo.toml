[package]
name = "tonic"
version = "0.1.0"
authors = ["CeresDB Authors <ceresdbservice@gmail.com>"]

[package.edition]
workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

# Rename to workaround doctest bug
# See: https://github.com/rust-lang/cargo/issues/6819

[dependencies]
upstream = { version = "0.6.2", package = "tonic", features = ["compression"] }
